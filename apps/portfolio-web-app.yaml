apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: portfolio-web
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.io
spec:
  project: default
  source:
    repoURL: git@github.com:wownae-ct/infra-repo.git
    targetRevision: HEAD
    path: apps/portfolio-web
  destination:
    server: https://kubernetes.default.svc
    namespace: portfolio-web
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
